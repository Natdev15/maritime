version: '3.8'

services:
  maritime-tracker:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Mount data directory for database persistence
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped 